import Desktop from "./header/desktop.hydrate.jsx";
import Mobile from "./header/mobile.hydrate.jsx";

<header class="sticky top-0 mb-8 bg-white shadow">
  <div class="mx-auto flex h-16 max-w-6xl items-center justify-between gap-4 px-4 md:justify-start">
    <a class="flex-shrink-0" href="/">
      {props.site.name}
    </a>
    <is-land
      land-on:interaction
      type="preact"
      import="/_includes/partials/header/desktop.hydrate.js"
      props={JSON.stringify({ class: "hidden md:block", nav: props.nav })}
    >
      <Desktop class="hidden md:block" nav={props.nav} />
    </is-land>
    <is-land
      land-on:interaction
      type="preact"
      import="/_includes/partials/header/mobile.hydrate.js"
      props={JSON.stringify({
        class: "ml-auto flex items-center md:hidden",
        nav: props.nav,
      })}
    >
      <Mobile class="ml-auto flex items-center md:hidden" nav={props.nav} />
    </is-land>
  </div>
</header>
