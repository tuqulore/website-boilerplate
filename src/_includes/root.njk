<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    {% include 'partials/ogp.njk' %}
    <link rel="stylesheet" href="{{ '/style.css' | url }}" />
    <title>{{ title }} | {{ site.name }}</title>
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>
